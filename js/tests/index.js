'strict';

const chai = require('chai');
const { expect } = chai;
const OptimisticRollIn = require('../src');

const test = () => {
  expect(true).to.be.true;
};

describe('Optimistic Roll Ins', () => {
  it('should do.', () => {
    test();
  });
});
